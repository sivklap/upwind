{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Python: iam_classifier",
        "type": "python",
        "request": "launch",
        "program": "${file}",  // runs the currently open script
        "console": "integratedTerminal"
      }
    ]
  }
  